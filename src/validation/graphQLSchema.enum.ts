export enum GraphQLSchemaErrorMessages {
  URLRequired = 'URL is required',
  SDLRequired = 'SDL is required',
  KeyRequired = 'Pass key or remove the pair',
  ValueRequired = 'Pass value or remove the pair',
}
